# Wasabi PingKak 個人網站專案

這是 Wasabi PingKak（山葵冰角）的個人網站專案，使用現代化的前端技術棧建立，部署於 GitHub Pages。

## 專案概述

**專案名稱**: Pingkak-Site
**作者**: Wasabi PingKak（工程師 Vtuber）
**網站連結**: https://wasabipingkak.github.io/Pingkak-Site/
**專案定位**: 個人品牌網站，用於展示創作內容、頻道計劃、技術專案與聯絡資訊

## 技術棧

### 核心框架與工具
- **React**: ^19.2.1 - 前端 UI 框架
- **Vite**: ^7.0.4 - 建構工具與開發伺服器
- **React Router DOM**: ^7.10.1 - 路由管理（使用 HashRouter）
- **Tailwind CSS**: ^3.4.17 - CSS 框架
- **React Icons**: ^5.5.0 - 圖標庫

### 開發工具
- **ESLint**: ^9.30.1 - 程式碼檢查
- **PostCSS**: ^8.5.6 - CSS 處理器
- **gh-pages**: ^6.3.0 - GitHub Pages 部署工具

## 專案結構

```
Pingkak-Site/
├── public/
│   └── assets/                  # 靜態資源
│       ├── logo.png             # 網站 Logo
│       ├── hero-visual.jpg      # 首頁主視覺
│       ├── site_icon.png        # 網站圖標
│       ├── artist-saltcan.jpg   # 繪師照片
│       ├── rigger-zelo.jpg      # 建模師照片
│       ├── logo-berrysorbet.jpg # Logo 設計師照片
│       ├── LOGO_complete.png    # 完整 Logo
│       ├── draft.jpg            # 角色草圖
│       ├── model-front.png      # 三視圖（正面）
│       ├── model-back.png       # 三視圖（背面）
│       ├── og-cover.jpg         # Open Graph 封面
│       └── marshmallow-icon.jpg # 棉花糖圖標
│
├── src/
│   ├── components/              # React 元件庫
│   │   ├── layouts/
│   │   │   └── MainLayout.jsx   # 主要佈局元件
│   │   ├── CreatorCard.jsx      # 創作者卡片
│   │   ├── CreatorCredits.jsx   # 創作者致謝區塊
│   │   ├── FeaturedProjects.jsx # 精選專案展示
│   │   ├── Footer.jsx           # 頁尾
│   │   ├── GitHubRepoCard.jsx   # GitHub 專案卡片
│   │   ├── Header.jsx           # 導航欄（含自動隱藏功能）
│   │   ├── Hero.jsx             # 首頁英雄區塊
│   │   ├── PlanItemCard.jsx     # 計劃項目卡片
│   │   ├── PlanSectionSidebar.jsx # 計劃側邊欄
│   │   ├── ScrollToTopButton.jsx  # 回到頂部按鈕
│   │   └── SocialLinks.jsx      # 社群連結
│   │
│   ├── pages/                   # 頁面元件
│   │   ├── Home.jsx             # 首頁
│   │   ├── CreatorCreditsPage.jsx # 設計師介紹頁
│   │   ├── PlanPage.jsx         # 頻道計劃頁
│   │   └── ContactPage.jsx      # 聯絡頁面
│   │
│   ├── App.jsx                  # 應用主元件（路由配置）
│   ├── main.jsx                 # React 應用入口
│   └── index.css                # 全域樣式
│
├── index.html                   # HTML 模板
├── vite.config.js               # Vite 配置（base: '/Pingkak-Site/'）
├── tailwind.config.js           # Tailwind 配置（darkMode: 'class'）
├── postcss.config.js            # PostCSS 配置
├── eslint.config.js             # ESLint 配置
├── package.json                 # 專案依賴與腳本
├── README.md                    # 專案說明文件
├── LICENSE                      # 授權文件
└── .gitignore                   # Git 忽略檔案
```

## 網站功能與頁面

### 1. 首頁 (`/`)
**主要內容**:
- **Hero 區塊**:
  - 左側：個人介紹文字（山葵冰角 Wasabi PingKak - 軟體工程 × 文件創作）
  - 右側：角色主視覺圖
  - 社群連結（YouTube、Twitter、GitHub、Discord、Email）
- **精選專案卡片區**:
  1. 🗺️ VTMap 頻道旅圖｜Vtuber TrailMap
  2. 🌍 GeoPingKak
  3. ❄️ LeetCode 冷藏庫
  4. 🎧 音遊 Vtuber 名單
  5. 🛠️ OBS 設定筆記
  6. 📮 冰角的委託指南
- **ScrollToTopButton**: 滾動頁面時出現的回到頂部按鈕

### 2. 設計頁面 (`/creator-credits`)
**主要內容**:
- **繪師**: 🍭鹽can🍭 (@saltcan69210)
- **建模師**: 李安瑟羅Zelo🌻 (@LeeAnZelo)
- **LOGO 與標準字設計**: 莓果雪酪/ベリー (@berrysorbet_58)
- **設計資源展示**:
  - 完整 LOGO
  - 角色草圖（附下載連結）
  - 三視圖正面（附下載連結）
  - 三視圖背面（附下載連結）

### 3. 頻道計劃頁面 (`/plan`)
**已完成項目**:
- **初配信** (2023/07/30 ~ 2024/11/13)
  - 包含 YouTube 影片嵌入
  - 連結至初配信準備進度表（Notion）
- **初配信後日談** (2025/03/28)
  - 包含 YouTube 影片嵌入
  - 詳細回顧整個籌備過程

**進行中項目**:
- **LeetCode 解題冷藏庫** (2024/11/13 ~ 進行中)
  - 面試題庫詳解計劃
  - 包含 GitHub 專案卡片
- **VTMap 頻道旅圖** (2025/04/06 ~ 進行中)
  - Vtuber 頻道分析與導流工具
  - 包含網站連結與 GitHub 專案卡片

**籌備中項目**:
- **GeoGuessr 教學**: 新手教學資源與免費練習網站
- **GeoGuessr 直播企劃**: 自製主題地圖與企劃書

### 4. 聯絡頁面 (`/contact`)
**主要內容**:
- 個人頭像展示
- 聯絡說明文字
- 商業合作指引
- 社群連結區塊

## 核心功能特性

### 1. 深色模式支援
- 使用 Tailwind CSS 的 `class` 模式切換
- 設定儲存於 localStorage
- 全站元件統一使用 `dark:` 前綴樣式
- 背景色統一使用 `dark:bg-gray-900/80`
- 切換按鈕：太陽圖標（深色模式）/ 月亮圖標（淺色模式）

### 2. 響應式設計 (RWD)
- 支援手機、平板、桌面三種尺寸
- 使用 Tailwind 的斷點系統：`sm:`、`md:`、`lg:`
- 手機版使用漢堡選單
- 桌面版使用水平導航列

### 3. Header 自動隱藏
- 向下捲動時淡出（`opacity-0`）
- 向上捲動時淡入（`opacity-100`）
- 使用 `useEffect` + `scroll` 事件監聽
- 背景使用半透明模糊效果：`bg-white/80 dark:bg-gray-900/80 backdrop-blur`

### 4. 回到頂部按鈕 (ScrollToTopButton)
- 捲動到一定距離後顯示
- 點擊平滑滾動至頂部
- 固定在右下角

### 5. 路由系統
- 使用 **HashRouter**（因 GitHub Pages 不支援 BrowserRouter）
- 路由配置：
  - `/` → Home
  - `/creator-credits` → CreatorCreditsPage
  - `/plan` → PlanPage
  - `/contact` → ContactPage

### 6. 外部內容嵌入
- YouTube 影片嵌入（透過 iframe）
- GitHub 專案卡片（GitHubRepoCard）
- 外部連結（Notion、Twitter、個人網站等）

## 開發指令

```bash
# 安裝依賴
npm install

# 啟動開發伺服器（預設 http://localhost:5173）
npm run dev

# 建立正式版本（輸出至 dist/）
npm run build

# 預覽建立後的版本
npm run preview

# 執行 ESLint 檢查
npm run lint

# 部署至 GitHub Pages
npm run deploy
```

## 部署流程

### GitHub Pages 配置
1. **Repository**: WasabiPingKak/Pingkak-Site
2. **Branch**: `gh-pages` (由 `npm run deploy` 自動建立)
3. **Base URL**: `/Pingkak-Site/` (在 `vite.config.js` 中設定)
4. **路由模式**: HashRouter (避免 GitHub Pages 404 問題)

### 部署步驟
```bash
# 1. 建立正式版本
npm run build

# 2. 部署至 gh-pages 分支
npm run deploy
```

`npm run deploy` 指令會：
- 將 `dist/` 資料夾的內容推送到 `gh-pages` 分支
- GitHub Pages 會自動偵測並部署

## 設計原則與注意事項

### CSS 設計原則
- **深色模式**: 統一使用 `dark:bg-gray-900/80` 作為背景色
- **過渡效果**: 使用 `transition` 或 `transition-colors` 確保平滑切換
- **邊框**: 深色模式使用 `dark:border-gray-700` 或 `dark:border-gray-800`
- **文字顏色**:
  - 標題：`text-gray-900 dark:text-white`
  - 內文：`text-gray-700 dark:text-gray-300`
- **懸停效果**: 使用 `hover:shadow-lg`、`hover:bg-blue-700` 等

### 效能考量
- Vite 的快速熱重載 (HMR)
- 生產環境自動進行程式碼分割與最小化
- 使用 React 19 的最新效能優化

### 可訪問性 (Accessibility)
- 使用語義化 HTML 標籤（`<header>`, `<nav>`, `<section>`, `<footer>`）
- 按鈕使用 `aria-label` 屬性
- 外部連結使用 `rel="noopener noreferrer"`

## 未來擴展方向

### 可能的功能增強
1. **部落格系統**: 新增 `/blog` 頁面，展示技術文章
2. **專案過濾**: 在精選專案區增加分類篩選功能
3. **多語言支援**: 新增英文版本
4. **動畫效果**: 使用 Framer Motion 增加頁面過渡動畫
5. **SEO 優化**: 改善 meta 標籤與結構化資料
6. **RSS Feed**: 提供訂閱功能

### 內容更新方向
- 持續更新頻道計劃進度
- 新增更多精選專案
- 補充技術筆記與教學文件

## 維護指南

### 如何新增精選專案
編輯 `src/components/FeaturedProjects.jsx`：
```javascript
const projects = [
  {
    title: "🆕 新專案名稱",
    description: "專案簡介",
    link: "https://example.com",
  },
  // ... 其他專案
];
```

### 如何新增計劃項目
編輯 `src/pages/PlanPage.jsx`，在對應的 section 中加入 `<PlanItemCard>` 元件。

### 如何更新社群連結
編輯 `src/components/SocialLinks.jsx`。

### 如何更新設計師資訊
編輯 `src/components/CreatorCredits.jsx` 中的 `creators` 陣列。

## 授權

詳見 LICENSE 檔案。

## 聯絡資訊

- **YouTube**: [Wasabi PingKak](https://www.youtube.com/@WasabiPingKak)
- **Twitter/X**: [@wasabi_pingkak](https://twitter.com/wasabi_pingkak)
- **GitHub**: [WasabiPingKak](https://github.com/WasabiPingKak)
- **Email**: wasabipingkak@gmail.com

---

*最後更新: 2026-01-19*
